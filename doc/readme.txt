Configuration tasks:

1. Enable and configure the External Authentication Filter in
   external/htmlroot/WEB-INF/web.xml (see example).

2. Add an authentication configuration to internal/cfg/LucyAuth.cfg
   (see example).

3. Modify the internal/cfg/om.cfg to choose the appropriate configuration
   for Web logins.
